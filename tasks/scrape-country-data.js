const Promise = require('bluebird');
const nightmare = require('nightmare');

module.exports = function(country) {
  //TODO Implement wikipedia scraping using Nightmare
  return Promise.resolve(country);
};
